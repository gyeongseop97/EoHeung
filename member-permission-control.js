(function(){
  const URL='https://chaddxsntnokjjcrwiyb.supabase.co';
  const KEY='sb_publishable_NiKj0BxbW3VauGK_kkflbg_OqMXPpCT';
  const LABEL={associate:'준회원',regular:'정회원',admin:'관리자'};
  const $=(s,r=document)=>r.querySelector(s);
  const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
  const low=s=>String(s||'').trim().toLowerCase();
  let client=null,user=null,members=[],busy=false,lastKey='',rendering=false,observer=null;
  function c(){if(!client&&window.supabase)client=window.supabase.createClient(URL,KEY);return client}
  function toast(m){const el=document.getElementById('toast');if(el){const d=document.createElement('div');d.textContent=m;el.appendChild(d);setTimeout(()=>d.remove(),3200)}else console.warn(m)}
  function style(){if($('#simpleMemberPermStyle'))return;const s=document.createElement('style');s.id='simpleMemberPermStyle';s.textContent='.perm-pill{display:inline-flex;align-items:center;border-radius:999px;padding:4px 8px;font-size:11px;font-weight:900}.perm-pill.associate{background:#f3f4f6;color:#4b5563}.perm-pill.regular{background:#e8fff6;color:#047857}.perm-pill.admin{background:#eef4ff;color:#074ca1}.account-cell{font-size:12px;color:#64748b;line-height:1.45}.member-role-select{min-width:104px}.signup-required{border-color:#f59e0b!important;background:#fffbeb!important}.member-perm-table th,.member-perm-table td{vertical-align:middle}#openMemberModalBtn{display:none!important}';document.head.appendChild(s)}
  function role(m){return m?.member_role||'associate'}
  function me(){const uid=String(user?.id||''),email=low(user?.email);return members.find(m=>String(m.auth_user_id||'')===uid)||members.find(m=>low(m.email)===email)||null}
  function isAdmin(){const mine=me();if(mine&&role(mine)==='admin')return true;return !members.some(m=>role(m)==='admin')}
  function opts(v){return ['associate','regular','admin'].map(x=>`<option value="${x}" ${x===v?'selected':''}>${LABEL[x]}</option>`).join('')}
  async function load(){const cl=c();if(!cl)return;const s=await cl.auth.getSession();user=s.data.session?.user||null;if(!user)return;const r=await cl.from('members').select('*').order('name');if(!r.error)members=r.data||[];await ensureSignup();lastKey='';render(true)}
  function bindSignup(){const pass=$('#loginPassword');if(!pass)return;if(!$('#signupName')){const i=document.createElement('input');i.id='signupName';i.className='input';i.placeholder='회원가입 이름';pass.insertAdjacentElement('afterend',i)}if(!$('#signupPhone')){const i=document.createElement('input');i.id='signupPhone';i.className='input';i.placeholder='회원가입 연락처';$('#signupName').insertAdjacentElement('afterend',i)}const b=$('#signUpBtn');if(b&&!b.dataset.simpleProfile){b.dataset.simpleProfile='1';b.addEventListener('click',e=>{const name=$('#signupName')?.value.trim()||'',phone=$('#signupPhone')?.value.trim()||'';$('#signupName')?.classList.toggle('signup-required',!name);$('#signupPhone')?.classList.toggle('signup-required',!phone);if(!name||!phone){e.preventDefault();e.stopImmediatePropagation();toast('회원가입 이름과 연락처를 입력해 주세요.');return}localStorage.setItem('eoheung_signup_profile',JSON.stringify({name,phone,pending:true}));},true)}}
  async function ensureSignup(){if(!user||busy)return;let d={};try{d=JSON.parse(localStorage.getItem('eoheung_signup_profile')||'{}')}catch(e){}if(!d.pending)return;busy=true;try{const uid=String(user.id||''),email=low(user.email);let found=members.find(m=>String(m.auth_user_id||'')===uid)||members.find(m=>low(m.email)===email);if(found){const patch={};if(!found.auth_user_id)patch.auth_user_id=uid;if(!found.email)patch.email=email;if(!found.phone&&d.phone)patch.phone=d.phone;if(Object.keys(patch).length)await c().from('members').update(patch).eq('id',found.id);localStorage.removeItem('eoheung_signup_profile');return}const payload={name:d.name||email.split('@')[0],phone:d.phone||'',email,auth_user_id:uid,member_role:'associate',status:'active'};const r=await c().from('members').insert(payload).select().single();if(!r.error&&r.data){members.push(r.data);localStorage.removeItem('eoheung_signup_profile');toast('준회원으로 등록되었습니다.')}}finally{busy=false}}
  function render(force=false){const root=$('#memberRows');if(!root)return;style();const table=root.closest('table');if(table)table.classList.add('member-perm-table');const head=table?.querySelector('thead tr');const key=JSON.stringify(members.map(m=>[m.id,m.name,m.phone,m.email,m.auth_user_id,role(m),m.memo]));const already=root.dataset.simplePerm==='1'&&root.querySelector('[data-role-member],.perm-pill');if(!force&&already&&key===lastKey)return;lastKey=key;rendering=true;root.dataset.simplePerm='1';if(head)head.innerHTML='<th>이름</th><th>연락처</th><th>가입 계정</th><th>권한</th><th>메모</th>';const admin=isAdmin();root.innerHTML=members.map(m=>{const r=role(m);return `<tr><td><b>${esc(m.name)}</b></td><td>${esc(m.phone||'')}</td><td><div class="account-cell"><b>${esc(m.email||'-')}</b><br>${m.auth_user_id?'연동 완료':'미연동'}</div></td><td>${admin?`<select class="input member-role-select" data-role-member="${m.id}">${opts(r)}</select>`:`<span class="perm-pill ${r}">${LABEL[r]||r}</span>`}</td><td>${esc(m.memo||'')}</td></tr>`}).join('')||'<tr><td colspan="5" class="empty">회원이 없습니다.</td></tr>';setTimeout(()=>{rendering=false},0)}
  function watchLegacyRenders(){const root=$('#memberRows');if(!root||observer)return;observer=new MutationObserver(()=>{if(rendering)return;if(!root.querySelector('[data-role-member],.perm-pill')&&members.length){setTimeout(()=>render(true),0)}});observer.observe(root,{childList:true,subtree:false});}
  document.addEventListener('change',async e=>{const sel=e.target.closest('[data-role-member]');if(!sel)return;if(!isAdmin())return toast('관리자만 권한을 변경할 수 있습니다.');const id=sel.dataset.roleMember,val=sel.value;const old=members.find(m=>String(m.id)===String(id));const r=await c().from('members').update({member_role:val}).eq('id',id).select().single();if(r.error){toast('권한 저장 오류: '+r.error.message);if(old)sel.value=role(old);return}if(old)old.member_role=val;lastKey='';render(true);toast('권한을 변경했습니다.')},true);
  function boot(){style();bindSignup();watchLegacyRenders();load();setInterval(()=>render(false),500);setInterval(load,30000)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
